# Azure AI Foundry Configuration
# Uses Entra ID (Azure AD) authentication - no API keys required
# Ensure you are logged in with: az login

# Optional: Specify tenant ID if using multiple tenants
# AZURE_TENANT_ID=your_tenant_id

# Optional: Override configuration from config.json
# AZURE_AI_FOUNDRY_PROJECT_ENDPOINT=https://YOUR_PROJECT.api.azureml.ms
# EXTRACTION_MODEL=gpt-4o
# MCP_SERVER_PORT=8000
# MIN_CONFIDENCE_THRESHOLD=0.8
# MAX_BUFFER_SIZE_MB=10
